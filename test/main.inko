import std.env
import std.test.(Filter, Tests)

import url_search_params.test_url_search_params

class async Main {
  fn async main {
    let tests = Tests.new

    test_url_search_params.tests(tests)

    tests.filter = Filter.from_string(env.arguments.opt(0).unwrap_or(''))
    tests.run
  }
}
